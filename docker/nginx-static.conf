server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Keep redirects relative so host/port from the browser origin survive
    # when nginx normalizes directory URLs like /tags -> /tags/.
    absolute_redirect off;

    location / {
        try_files $uri $uri/ =404;
    }
}
