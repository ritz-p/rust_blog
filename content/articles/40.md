---
title: TypeScript and TSX Syntax Highlighting
slug: '40'
created_at: 2026-09-25T00:00:00Z
excerpt: 型・ジェネリクス・JSX と埋め込み式を確認します。
tags:
- TypeScript
categories:
- test
---

```typescript
type Result<T> = { value: T; ok: boolean };

function wrap<T>(value: T): Result<T> {
  return { value, ok: true };
}

const result = wrap<string>("世界 <tag> & value");
console.log(result.value);
```

```tsx
type Props = { names: string[] };

export function Greetings({ names }: Props) {
  return (
    <ul className="greetings">
      {names.map(name => <li key={name}>{name} &amp; friends</li>)}
    </ul>
  );
}
```
