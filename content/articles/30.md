---
title: "タグの正規化と抜粋変換"
slug: "30"
excerpt: "~~削除部分~~ **強調** と [https://www.google.com]、[リンク](https://example.com \"ツールチップ\")"
tags: ["Rust", "rust", "C#", "c#"]
categories: ["Test", "test"]
---

この記事は seed と抜粋変換の確認専用です。長文や表の表示レイアウトは記事18で確認します。

## タグ・カテゴリの正規化

front matter に指定した `Rust` と `rust`、`C#` と `c#`、`Test` と `test` が、それぞれ同じタグ・カテゴリとして登録されることを確認します。seed を再実行しても記事との関連が重複しないことを確認します。

新規登録時の表示名は最初の表記を保持します。`blog_config.toml` に表示名が指定されている場合は、その設定を優先します。

## 記号を含むタグのリンク

`C#` タグのリンクが `/tag/c%23/` に遷移し、タグ一覧からこの記事を開けることを確認します。`#` がページ内リンクとして扱われないことを確認します。

## 明示指定した抜粋の変換

記事一覧・タグ別・カテゴリ別の一覧で、front matter の抜粋が次のように表示されることを確認します。

強調 と https://www.google.com、リンク

取り消し線の内容、強調記号、URL の角括弧、リンクのツールチップが抜粋に残らないことを確認します。
