{% if latest_articles and latest_articles | length > 0 %}
    <aside class="menu">
        <p class="menu-label">
            {% if surrounding_articles is defined and surrounding_articles %}前後の記事{% else %}📰 最新記事{% endif %}
        </p>
        <ul class="menu-list">
            {% for latest_article in latest_articles %}
            <li>
                <a
                href="{{ latest_article.url }}"
                >
                {{ latest_article.title }}
                </a>
            </li>
            {% endfor %}
        </ul>
    </aside>
{% endif %}
